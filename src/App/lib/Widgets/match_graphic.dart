import 'package:flutter/cupertino.dart';

class MatchGraphic extends StatelessWidget{
  const MatchGraphic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}